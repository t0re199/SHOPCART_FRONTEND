import { SharedHttpResources } from './shared-http-resources';

describe('SharedHttpResources', () => {
  it('should create an instance', () => {
    expect(new SharedHttpResources()).toBeTruthy();
  });
});
